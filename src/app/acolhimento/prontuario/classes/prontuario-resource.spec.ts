import { ProntuarioResource } from "./prontuario-resource";


describe('PasResource', () => {
  it('should create an instance', () => {
    expect(new ProntuarioResource(null)).toBeTruthy();
  });
});
